{
  "name": "crypto-wallet-generator-frontend",
  "version": "1.0.0",
  "description": "Frontend for crypto wallet generator",
  "main": "index.html",
  "scripts": {
    "start": "live-server --port=3000 --host=0.0.0.0",
    "build": "echo 'No build process needed for static files' && exit 0",
    "dev": "live-server --port=3000"
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "homepage": "."
}